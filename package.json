{
  "name": "scrape-tesda",
  "version": "1.0.0",
  "description": "TESDA certificate scraping API using Playwright + Express",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "1.42.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
